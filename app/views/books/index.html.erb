<h1>Books</h1> <!--Book投稿一覧画面-->
<% @books.each do |book| %>
<div>

  <p>投稿ユーザー画像：</p>
  <p>ユーザーネーム：<%= book.user.name %></p>

  <p>Title：</p>
  <%= link_to book_path(book.id) do %>
  <%= book.title %>
  <% end %>
  <p>Opinion：<%= book.body %></p>
</div>
<% end %>